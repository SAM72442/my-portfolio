generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Project {
  id        Int      @id @default(autoincrement())
  title     String
  desc      String
  image     String
  tags      String[]
  createdAt DateTime @default(now())
}